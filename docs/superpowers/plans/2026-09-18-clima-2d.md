# Plan — clima 2D (WeatherFX)

Spec: [../specs/2026-09-18-clima-2d-design.md](../specs/2026-09-18-clima-2d-design.md). TDD por tarea; `npm run check` verde antes de cada commit.

1. **Vendorizar** — `public/js/vendor/pixi.min.js` (7.4.2 + LICENSE) y `public/js/vendor/weather-fx.js`
   (mover desde `weather-fx/`; el demo apunta al vendor). Test: `frontend.test.js` comprueba cabecera MIT y versión.
2. **Servidor** — `rules.js`: `WEATHER_IDS`, `cleanSettings.weather`. Tests en `rules.test.js`.
3. **Cliente datos** — `core.js WEATHERS` + `blankState.weather`; `net.js SCENE_KEYS`. Test: ids iguales en
   `core.js`, `rules.js` y `vendor/weather-fx.js`; `SCENE_KEYS` contiene `weather`.
4. **`weather.js`** — módulo puente (carga perezosa, montar/cambiar/destruir, PERF, animate). Test: compila;
   `index.html` lo carga antes de `render.js`.
5. **Enganches** — `render.js`: `Weather.sync()` en `drawAll`, `Weather.invalidate()` tras `drawScene`,
   `Weather.resize()` en `resize`, `syncStageMode`. Test: regex en `frontend.test.js`.
6. **Panel Escena** — `index.html` select + 2 rangos; `editor.js` handlers y `syncSceneInputs`. Test: marcado.
7. **`test:ui`** — caso lluvia/ninguno en director y jugador con captura.
8. **Docs** — 01 (capa), 02 (función), 05 (gotcha carga perezosa), 06, 07.
